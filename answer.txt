12. --upgrade 13.whl 14.--main-- 15. abcd 16. PIL.Image 17.tkinter.messagebox 
18. 4 19. builtin_module_names 20.stdlib_module_names 21.modules 22.exec_prefix
23.executable 24.path 25.argv 26.danhang: #,duohuang:''' 27.\ 31. random(dont have yuan-chengxu file) 32.pillow( tuxiang-chuli ku) 34.demo 35.py,pyw 36.abcd 
37.abcd 38.abcd 39.abc 40.abcd 41.abcd 42.abcd 43.abcd 44.cd 45. ab 都是操作系统的命令行工具，其中power shell 是高级命令行工具，idlepython 交互模式只能运行代码，d则能通过控制台用前缀！直接执行系统命令 46.d 47.ab 48~51:import只能最后只能是模块名，from import最后可以是模块名和对象名，其中对象名可以是函数名，方法名等，(可以看作是模块的API?)，而math是标准库函数的模块，sin是math模块中的函数对象，from math import *中的*是一种语法符号，表示导入模块中的公开成员(公开api?)，不是一个对象54 背后的原因要搞清楚 68.取决于系统环境变量(path)中python解释器（路径）的位置73.false 两个命令是独立的，第二个命令无法访问第一个命令中的变量76.可以明确指定路径(怎么明确指定路径？还有前面提到的对python解释器文件重命名，重命名后具体怎么操作？问问gpt) 77../执行当面目录中的命令和程序(power shell 中) 94 scripts文件夹存放包管理工具（比如pip.exe，文件扩展名是exe）与带命令行的第三方库（如jupyter），且不同版本目录下scripts、文件夹中存放的工具版本和位数以来是不同的，因此建议pip在对应版本下的scripts目录中执行100.(本题很重要)，pip本质是系统级可执行程序，只有能调用系统命令的工具（如命令行和power shell）上才能执行pip，而pip不是python语法，idle实质是python解释器的图形化界面，功能是把上面的代码交给python解释器执行，因为只能执行python代码，而pip命令是系统级可执行程序而非python语法，故不能在idle上执行pip命令，之所以很多人误认为pip命令是python语法是因为其和python强绑定，只能用来管理python库，且其执行文件pip.exe（window中）又在python目录下的子文件夹scripts下114.对，可以同时安装多个扩展库115.可以使用==指定版本，如pip install numpy == 版本号121.在idle环境中，使用分号分割可以一次执行多条简单语句，但不能一次执行多个循环结构，选择结构等复合语句 128.关于同一条语句换行的问题，可以用（）把一条语句包起来，也可以用\换行，但不能在一个变量内部用换行\，而列表元素换行时用,就能换，不需要续行符\,并且一行中\后面不能有任何内容 137 python注释有#和三引号(137-141多读代码就能逐步习惯)
